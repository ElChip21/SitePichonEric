{% extends 'base.html.twig' %}

{% block title %}Temoignage{% endblock %}

{% block body %}
    <h1>Temoignage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ temoignage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ temoignage.name }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ temoignage.contenu }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ temoignage.date ? temoignage.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_temoignage_index') }}">back to list</a>

    <a href="{{ path('app_temoignage_edit', {'id': temoignage.id}) }}">edit</a>

    {{ include('temoignage/_delete_form.html.twig') }}
{% endblock %}
